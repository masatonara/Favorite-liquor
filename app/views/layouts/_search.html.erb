<div class="search-form">
  <%= form_with url: search_path, method: :get, local: true do |f| %>
    <%= f.text_field :content %>
    <%= f.select :model, options_for_select({ "ユーザー検索" => "user", "投稿検索" => "liquor"}) %>
    <%= f.select :method, options_for_select({ "キーワード" => "partial", "完全一致" => "perfect", "前方一致" => "forward", "後方一致" => "backward" }) %>
    <%= f.submit '検索' %>
  <% end %>
</div>